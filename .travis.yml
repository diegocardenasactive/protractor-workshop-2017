language: node_js
notifications:
  email: false
node_js:
 - '7'
branches:
 except:
  - /^v\d+\.\d+\.\d+$/
sudo: required
dist: trusty
addons:
  chrome: stable
before_install:
  - google-chrome-stable --headless --disable-gpu --remote-debugging-port=9222 http://localhost &
